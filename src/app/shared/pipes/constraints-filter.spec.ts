import {ConstraintsFilterPipe} from './constraints-filter.pipe';

describe('ConstraintsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstraintsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
