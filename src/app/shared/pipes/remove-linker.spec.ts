import {RemoveLinkerPipe} from './remove-linker.pipe';

describe('RemoveLinkerPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveLinkerPipe();
    expect(pipe).toBeTruthy();
  });
});
